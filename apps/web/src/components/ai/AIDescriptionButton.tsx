'use client';

interface Props {
  productName: string;
  category: string;
  keyFeatures: string[];
  onResult: (result: unknown) => void;
}

export function AIDescriptionButton(_props: Props) {
  return null;
}
